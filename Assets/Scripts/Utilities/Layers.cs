namespace Assets.Scripts.Utilities
{
    public static class Layers
    {
        public enum Layer
        {
            Default,
            TransparentFX,
            IgnoreRaycast,
            Player,
            Water,
            UI,
            Environment,
            Interactable
        }
    }
}
