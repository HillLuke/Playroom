namespace Assets.Scripts.Utilities
{
    public enum Layers
    {
        Default,
        TransparentFX,
        IgnoreRaycast,
        Player,
        Water,
        UI,
        Environment,
        Interactable
    }
}
